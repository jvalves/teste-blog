import { PostParagPipe } from './post-parag.pipe';

describe('PostParagPipe', () => {
  it('create an instance', () => {
    const pipe = new PostParagPipe();
    expect(pipe).toBeTruthy();
  });
});
