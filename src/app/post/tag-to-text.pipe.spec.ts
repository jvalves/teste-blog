import { TagToTextPipe } from './tag-to-text.pipe';

describe('TagToTextPipe', () => {
  it('create an instance', () => {
    const pipe = new TagToTextPipe();
    expect(pipe).toBeTruthy();
  });
});
